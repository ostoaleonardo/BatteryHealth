<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BatteryHealth" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BatteryHealth" parent="Base.Theme.BatteryHealth" />

    <style name="AppBar.App.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="fontFamily">@font/azeret_mono_light</item>
        <item name="android:fontFamily">@font/azeret_mono_light</item>
        <item name="textAllCaps">true</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>